{
  "name": "@channelcheckr/shared",
  "version": "0.1.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rimraf dist coverage tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^4.5.5"
  }
}
